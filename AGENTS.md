# Agent instructions

## Agent skills

### Issue tracker

Issues and PRDs live in this repo's **GitHub Issues** (`gh` CLI). See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical triage roles; label strings match role names unless overridden in the mapping. See `docs/agents/triage-labels.md`.

### Domain docs

**Single-context** — one `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
